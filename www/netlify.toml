# Base directory for this site is "www". Content comes from Sanity at build
# time, so a content change needs a rebuild — see the build hook wired to the
# Sanity webhook.
[build]
  command = "npm run build"
  publish = "dist"
