[build]
  command = "npm run build"
  publish = "dist"

# The Studio is a single-page app: every path must serve index.html so a deep
# link to a document survives a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
